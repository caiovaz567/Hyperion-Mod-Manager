// Tailwind CSS v4 is handled by the @tailwindcss/vite plugin (see electron.vite.config.ts),
// not by a PostCSS plugin. This file is intentionally left without plugins.
module.exports = {
  plugins: {}
}
